# Conf
plugin {
    enabled = true

    mode = rotate
    threshold = 10

    rotate {
        length = 10
    }

    tilt {
        limit = 5000
        function = negative_quadratic
    }

    shake = true

    shake {
        threshold = 3.0
        factor = 3.5
        effects = rotate
        nearest = true
    }
}
