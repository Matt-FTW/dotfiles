source = ../theme/colors.conf

# Conf
plugin {
    hyprtasking {
        rows = 3
        exit_behavior = interacted
        gap_size = 8
        bg_color = $surface0
    }
}

# Keybinds
bind = ALT, ESCAPE, hyprtasking:toggle, cursor
